http:
  routers:
    my-router:
      entryPoints:
        - web
      middlewares:
        - energyMonitorPlugin
      service: service-whoami
      rule: Path(`/whoami`)

  services:
    service-whoami:
      loadBalancer:
        servers:
          - url: http://localhost:5000
        passHostHeader: false

  middlewares:
    energyMonitorPlugin:
      plugin:
        energyMonitor: 
            Foo: Bar

